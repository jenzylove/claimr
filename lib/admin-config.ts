// Single source of truth for the obscured admin URL.
//
// If you ever want to rotate the slug, change this value AND rename the
// matching folder under app/. Sidebar links and the layout's pathname
// matcher both pick up this constant.

export const ADMIN_BASE_PATH = "/hq/c8m4z2";
